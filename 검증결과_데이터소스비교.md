# 조사 보고서 검증 결과 및 JLCPCB/LCSC 데이터 소스 비교 분석

## 1. 검증에서 발견된 오류 및 수정사항

### 1.1 LCSC 비공식 API - 폐쇄됨 / 공식 Partner API 존재 확인

**이전 보고서 내용 (오류)**:
> 비공식 API: `https://wwwapi.lcsc.com/v1/` - 인증 없이 GET 요청으로 검색 가능

**검증 결과**:

**(A) 구형 비공식 API — 폐쇄 확인:**
- `wwwapi.lcsc.com` → DNS 해석 실패 (ENOTFOUND). 도메인 자체가 더 이상 존재하지 않음
- `wmsc.lcsc.com/ftps/wm/search/global` → HTTP 403 Forbidden
- `wmsc.lcsc.com/wmsc/search/global` → 404 "The static resource is unavailable"
- 이전에 인증 없이 접근 가능했던 비공식 엔드포인트들은 모두 차단됨

**(B) 공식 LCSC Partner API — 존재 확인 (https://www.lcsc.com/docs/):**
- LCSC는 파트너 대상 공식 API를 제공하고 있음
- 8개의 API 서비스 제공 (Keyword Search, Category, Item Details 등 포함)
- **신청 조건**: LCSC 계정 + 사업자등록증 + 회사 웹사이트 + 예상 주문량 등 제출
- **인증**: key + nonce + timestamp + SHA1 signature. IP 화이트리스트 필수.
- **Rate Limit**: 분당 한도(429), 일별 한도(430) — 정확한 수치 비공개
- **이용약관 핵심 제약**:
  - 대량 데이터 수집(bulk data capture) 명시적 금지
  - 제3자에게 데이터 호스팅/제공 금지 (데이터시트, 이미지 포함)
  - 데이터 또는 파생 데이터 판매 금지
  - **다른 공개 API나 상업적 데이터 서비스에 통합 금지**
  - LCSC를 출처로 명시 의무
  - API 기술 문서를 제3자와 공유 금지

**결론**: LCSC 공식 Partner API가 존재하지만, 이용약관이 매우 엄격하여 우리 프로젝트(공개 웹사이트에서 데이터 제공)의 목적과 충돌합니다. 특히 "다른 공개 API/상업적 데이터 서비스에 통합 금지" 및 "제3자에게 데이터 호스팅/제공 금지" 조항은 공개 검색 웹사이트로의 활용을 사실상 제한합니다.

---

### 1.2 JLCPCB 공식 API - 접근 조건이 매우 제한적

**이전 보고서 내용 (부정확)**:
> 개발자 포털에서 APP_ID, API_KEY, API_SECRET 발급

**검증 결과**:
JLCPCB 공식 API(https://api.jlcpcb.com/)의 실제 접근 조건:
- **심사 기반**: 모든 신청이 승인되지 않음
- **심사 기준**: 이전 JLCPCB 주문 이력, 회사 상황, 비즈니스 관계
- **제약**: URL에 'JLC' 문자 사용 금지, JLCPCB 상표/로고 사용 금지
- **위반 시**: API 접근 즉시 중단 및 연결된 모든 계정 정지
- **개인 프로젝트**: 승인 가능성 낮음

**결론**: JLCPCB 공식 API는 일반 개발자/개인 프로젝트에서 직접 사용하기 **매우 어렵습니다**. 기업 대상이며 파트너 관계가 전제됩니다.

---

### 1.3 jlcsearch API - 검증 완료, 정상 작동

**검증 결과** (2026-08-13 직접 테스트):

```
GET https://jlcsearch.tscircuit.com/resistors/list.json?package=0603&resistance=10000
→ 200 OK, 풍부한 데이터 반환 (60+ 부품)

GET https://jlcsearch.tscircuit.com/api/search?q=STM32F103&limit=2
→ 200 OK, {"components":[{"lcsc":8734,"mfr":"STM32F103C8T6","package":"LQFP-48(7x7)","stock":214596,"price":1.037},...]}

GET https://jlcsearch.tscircuit.com/categories/list.json
→ 200 OK, 120+ 카테고리/서브카테고리 목록
```

**제공 데이터 필드** (resistors 엔드포인트 기준):
- `lcsc`: LCSC 부품 번호 (숫자, C 접두사 없음)
- `mfr`: 제조사 부품 번호 (MPN)
- `package`: 패키지 (0402, 0603, 0805 등)
- `stock`: 현재 재고 수량
- `price1`: 최소 수량 단가 (USD)
- `in_stock`: 재고 여부
- `is_basic`: JLCPCB Basic 부품 여부
- `is_preferred`: Preferred 부품 여부
- `resistance`: 저항값 (옴 단위, 정규화됨)
- `tolerance_fraction`: 허용오차 (0.01 = ±1%)
- `power_watts`: 정격 전력 (와트)
- `max_overload_voltage`: 최대 과부하 전압
- `is_surface_mount`: SMD 여부
- `attributes`: 원시 속성 JSON 문자열

**확인된 엔드포인트**:
- `/resistors/list.json` - 파라미터: package, resistance, tolerance 등
- `/capacitors/list.json` - 파라미터: package, capacitance, voltage 등
- `/microcontrollers/list.json`
- `/voltage_regulators/list.json`
- `/api/search?q=&limit=` - 전체 검색 (component_catalog 내 모든 in-stock 부품 대상)
- `/categories/list.json` - 카테고리 목록
- 45개 derived table 카테고리별 전용 엔드포인트 (파라미터 검색 가능)

**인증**: 불필요 (완전 무료, 공개 API)
**Rate Limit**: 명시되지 않음 (Cloudflare Workers 기반)
**데이터 신선도**: nightly stock sync (05:00 UTC), 메인 브랜치 머지 시 전체 카탈로그 업데이트
**데이터 범위 주의**: jlcsearch의 데이터는 yaqwsx/jlcparts의 SQLite DB에서 파생됨. jlcparts는 JLCPCB OpenAPI를 통해 점진적으로 수집하며 (하루 3회, 시간 제한 있음), 1년 이상 재고 없는 부품은 삭제됨. 따라서 "JLCPCB Assembly 라이브러리 전체"라고 보장하기 어려우며, "jlcparts가 현재 인덱싱하고 있는 in-stock 부품"으로 이해해야 함.

---

### 1.4 Mouser/DigiKey/element14 - 이전 조사 대체로 정확

**DigiKey**: Product Information V4 API, OAuth 2.0, 120 req/min + 1000 req/day → 확인됨
**Mouser**: Search API, API Key 기반, SOAP+REST → 확인됨 (정확한 rate limit 미공개)
**element14**: REST API, partner.element14.com, API Key 기반, 페이징 100개 제한 → 확인됨

---

## 2. JLCPCB/LCSC 데이터 소스 옵션 비교

첫 번째 데이터 소스로 JLCPCB/LCSC를 사용할 경우, 가능한 방식은 5가지입니다.

### 옵션 A: jlcsearch API 직접 사용 (tscircuit/jlcsearch)

| 항목 | 내용 |
|------|------|
| **방식** | HTTPS GET 요청으로 JSON 응답 |
| **인증** | 불필요 |
| **데이터 범위** | yaqwsx/jlcparts가 수집한 JLCPCB 부품 중 in-stock 데이터 (전체 coverage 보장 아님) |
| **파라미터 검색** | O (45개 derived table 카테고리에 한해 지원) |
| **전체 검색** | O (/api/search — component_catalog 전체 대상, 키워드 기반) |
| **데이터 신선도** | 매일 업데이트 (재고는 nightly stock sync, 카탈로그는 main 머지 시) |
| **가격 정보** | price1 (최소 수량 단가만, 수량별 가격 래더 없음) |
| **설치/인프라** | 없음 (외부 API 호출만) |
| **비용** | 무료 |
| **안정성** | tscircuit 프로젝트 지속성에 의존 (활발히 유지 중) |
| **데이터 양** | jlcparts가 현재 인덱싱한 in-stock 부품 (README에서 "2.5M+" 언급) |

**장점**:
- 즉시 사용 가능, 인프라 불필요
- 파라미터 검색이 45개 카테고리에서 이미 구현됨 (저항값, 용량, 전압 등)
- 전체 검색(/api/search)은 모든 in-stock 부품 대상
- 카테고리별 최적화된 응답 구조
- 무료, 인증 불필요

**단점**:
- 외부 서비스 의존 (다운타임, 변경 위험)
- 수량별 가격 래더 없음 (단가만)
- 전체 JLCPCB Assembly 라이브러리 coverage가 코드상 보장되지 않음 (jlcparts의 점진적 수집 방식 + 1년 미재고 삭제)
- 파라미터 검색은 derived tables에 정의된 45개 카테고리만 (나머지는 키워드 검색만)
- 데이터시트 URL 없음 (search_index에 포함되지 않음)
- 커스터마이징 불가

---

### 옵션 B: CDFER/jlcpcb-parts-database SQLite 다운로드

| 항목 | 내용 |
|------|------|
| **방식** | GitHub Pages에서 SQLite 파일 다운로드 (~1GB) |
| **인증** | 불필요 |
| **데이터 범위** | 재고 5개 이상인 전체 부품 |
| **파라미터 검색** | 직접 구현 필요 (SQL 쿼리) |
| **데이터 신선도** | 매일 업데이트 (yaqwsx/jlcparts 업데이트 3시간 후) |
| **가격 정보** | 수량별 가격 래더 포함 |
| **설치/인프라** | SQLite DB 호스팅 필요 |
| **비용** | 무료 |
| **안정성** | 자체 보관 가능, GitHub 인프라에만 초기 의존 |
| **데이터 양** | 약 500만+ 부품 (재고 5개 이상) |

**장점**:
- 전체 데이터를 자체 인프라에서 관리 가능
- 수량별 가격 래더 포함
- FTS (Full-Text Search) 인덱스 이미 포함
- 외부 API 실시간 의존 없음
- 커스터마이징 자유

**단점**:
- 1GB 파일 다운로드/관리 필요
- 파라미터 검색은 직접 구현해야 함 (attributes는 JSON 문자열)
- 파라미터 파싱/정규화 로직 직접 작성 필요
- 업데이트 파이프라인 직접 구축 필요

---

### 옵션 C: yaqwsx/jlcparts 전체 DB 직접 활용

| 항목 | 내용 |
|------|------|
| **방식** | GitHub Releases에서 11GB multi-volume zip 다운로드 |
| **인증** | 불필요 |
| **데이터 범위** | JLCPCB 전체 부품 (재고 없는 것 포함) |
| **파라미터 검색** | 직접 구현 필요 |
| **데이터 신선도** | 하루 3회 업데이트 (03:00, 11:00, 19:00 UTC) |
| **가격 정보** | 수량별 가격 래더 포함 |
| **설치/인프라** | 대용량 스토리지 + 처리 인프라 필요 |
| **비용** | 무료 |
| **안정성** | 가장 원본에 가까운 데이터 |
| **데이터 양** | 약 700만 부품 |

**DB 스키마** (source-db-v2):
```sql
-- 핵심 테이블
jlc_components (
  lcsc INTEGER PRIMARY KEY,    -- LCSC 부품 번호 (숫자)
  category TEXT,               -- 1차 카테고리
  subcategory TEXT,            -- 2차 카테고리  
  mfr TEXT,                    -- 제조사 부품 번호 (MPN)
  package TEXT,                -- 패키지
  joints INTEGER,              -- 솔더 조인트 수
  manufacturer TEXT,           -- 제조사명
  library_type TEXT,           -- 'base' 또는 'expand'
  preferred INTEGER,           -- preferred 부품 여부
  last_on_stock INTEGER,       -- 마지막 재고 확인 timestamp
  description TEXT,            -- 설명
  datasheet TEXT,              -- 데이터시트 URL
  stock INTEGER,               -- 재고 수량
  price TEXT,                  -- "1-9:0.05,10-99:0.03,..." 형태
  attributes TEXT,             -- JSON: {"Resistance":"10kΩ","Tolerance":"±1%",...}
  rohs INTEGER,                -- RoHS 여부
  assembly_process TEXT,       -- SMT/DIP
  ...
)

lcsc_components (
  lcsc INTEGER PRIMARY KEY,
  manufacturer TEXT,           -- LCSC에서의 제조사명
  attributes TEXT,             -- LCSC 추가 속성 JSON
  image TEXT,                  -- 이미지 파일명
  url_slug TEXT,               -- LCSC 제품 페이지 slug
)
```

**장점**:
- 가장 완전한 데이터 (재고 없는 부품 포함)
- 하루 3회 업데이트로 가장 신선한 데이터
- 제조사명, 데이터시트 URL, 이미지 포함
- 수량별 가격 래더 포함
- jlcsearch가 이미 이 DB를 기반으로 동작하므로 참고 구현 풍부

**단점**:
- 11GB (7zip 멀티볼륨) - 다운로드/처리에 시간과 자원 필요
- 직접 최적화/인덱싱 필요
- attributes가 원시 JSON이므로 파라미터 파싱 로직 필요
- 인프라 비용 (스토리지, 처리)

---

### 옵션 D: JLCPCB 공식 API 직접 사용

| 항목 | 내용 |
|------|------|
| **방식** | HMAC-SHA256 인증된 POST 요청 |
| **인증** | APP_ID + ACCESS_KEY + SECRET_KEY (심사 기반 발급) |
| **데이터 범위** | 전체 JLCPCB 라이브러리 |
| **파라미터 검색** | 제한적 (목록 페이지네이션 + 상세 조회) |
| **데이터 신선도** | 실시간 |
| **가격 정보** | 실시간 수량별 가격 |
| **설치/인프라** | API 클라이언트 구현 필요 |
| **비용** | 무료 (승인 시) |
| **안정성** | 공식 API, 안정적 (접근 가능 시) |
| **데이터 양** | 전체 |

**장점**:
- 실시간 데이터 (가격, 재고)
- 가장 정확한 공식 데이터
- 안정적인 엔드포인트

**단점**:
- **접근 승인이 매우 어려움** (기업 대상, 주문 이력 심사)
- 브랜드/URL 사용 제한
- 검색 기능이 제한적 (파라미터 검색 미지원, 목록+상세 조회만)
- 위반 시 계정 정지 위험

---

### 옵션 D-2: LCSC 공식 Partner API 직접 사용

| 항목 | 내용 |
|------|------|
| **방식** | SHA1 서명 인증된 요청 (key+nonce+timestamp+signature) |
| **인증** | API Key + Secret (신청 기반 발급) + IP 화이트리스트 |
| **데이터 범위** | LCSC 전체 카탈로그 (64만+ 재고 부품) |
| **파라미터 검색** | 제한적 (keyword + categoryId + inStockOnly 등) |
| **데이터 신선도** | 실시간 |
| **가격 정보** | 실시간 가격 (ProductPricing 옵션) |
| **설치/인프라** | API 클라이언트 구현 필요, 고정 IP 필요 |
| **비용** | 무료 (승인 시) |
| **안정성** | 공식 API, 안정적 (접근 가능 시) |
| **데이터 양** | 전체 LCSC 카탈로그 |

**장점**:
- LCSC 공식 실시간 데이터 (가격, 재고)
- lastUpdateTime으로 변경분만 조회 가능
- keyword + categoryId 조합 검색
- 복수 통화 지원 (USD, CNY, EUR, HKD)

**단점**:
- **이용약관이 우리 프로젝트와 충돌**: "다른 공개 API/상업적 데이터 서비스에 통합 금지", "제3자에게 데이터 호스팅/제공 금지"
- 공개 웹사이트로 데이터를 제공하는 것이 약관 위반에 해당할 가능성 높음
- 사업자등록증 등 기업 자격 필요
- IP 화이트리스트 필요 (서버리스 환경 사용 어려움)
- Rate limit 구체적 수치 비공개

---

### 옵션 E: 하이브리드 (jlcsearch API + 자체 캐시 DB)

| 항목 | 내용 |
|------|------|
| **방식** | jlcsearch API를 주 데이터 소스로, 추가 데이터를 CDFER DB에서 보강 |
| **인증** | 불필요 |
| **데이터 범위** | jlcsearch 범위 + CDFER 추가 데이터 |
| **파라미터 검색** | jlcsearch의 40+ 카테고리별 필터 활용 |
| **데이터 신선도** | jlcsearch 수준 (매일) + 자체 캐시 |
| **가격 정보** | 단가(jlcsearch) + 래더(CDFER 보강) |
| **설치/인프라** | 캐시 DB + API 프록시 |
| **비용** | 무료 |
| **안정성** | jlcsearch 다운 시 자체 캐시로 폴백 |

**장점**:
- 빠른 시작 (jlcsearch API 즉시 활용)
- 파라미터 검색 기능을 처음부터 사용 가능
- CDFER DB로 가격 래더, 데이터시트 URL 등 보강
- jlcsearch 장애 시에도 자체 캐시로 서비스 지속
- 점진적 확장 가능

**단점**:
- 두 소스간 데이터 동기화 복잡성
- 초기에 두 시스템 모두 이해해야 함

---

## 3. 권장 사항

### 최종 권장: 옵션 E (하이브리드) → 점진적으로 옵션 C로 전환

**Phase 1 (MVP 단계)**: jlcsearch API를 프록시하여 즉시 시작
- jlcsearch API가 이미 파라미터 검색, 재고 필터, Basic/Preferred 구분을 제공
- 우리 백엔드는 jlcsearch 응답을 캐싱하고 추가 기능(비교, 선택 관리)만 구현
- 빠른 프로토타이핑 가능

**Phase 1.5**: CDFER SQLite로 데이터 보강
- 수량별 가격 래더 추가
- 데이터시트 URL 추가
- 제조사명 보강
- jlcsearch가 커버하지 않는 카테고리 데이터 추가

**Phase 2**: 자체 데이터 파이프라인 구축 (yaqwsx/jlcparts 기반)
- 자체 GitHub Actions로 데이터 업데이트
- tscircuit/jlcsearch의 derived table 구조 참고하여 자체 최적화 DB 구축
- jlcsearch 의존 제거, 완전 자체 운영

**이유**:
1. jlcsearch API는 **지금 당장** 작동하며 인증 없이 무료
2. JLCPCB 공식 API는 접근 자체가 불확실
3. LCSC 비공식 API는 폐쇄됨
4. yaqwsx/jlcparts + tscircuit/jlcsearch의 오픈소스 데이터 파이프라인은 검증되었고 활발히 유지됨
5. 하이브리드 접근으로 리스크 분산 + 빠른 시작이 가능

---

## 4. 요약 비교표

| 기준 | A: jlcsearch API | B: CDFER SQLite | C: jlcparts 전체 DB | D: JLCPCB 공식 | D-2: LCSC Partner | E: 하이브리드 |
|------|:-:|:-:|:-:|:-:|:-:|:-:|
| 즉시 사용 가능 | ★★★ | ★★ | ★ | ✗ | ★★ | ★★★ |
| 데이터 완전성 | ★★ | ★★ | ★★★ | ★★★ | ★★★ | ★★★ |
| 파라미터 검색 | ★★★(45카테고리) | ★ | ★ | ★ | ★★ | ★★★ |
| 가격 래더 | ✗ | ★★★ | ★★★ | ★★★ | ★★★ | ★★★ |
| 데이터시트 | ✗ | ★★★ | ★★★ | ★★★ | ★★★ | ★★★ |
| 외부 의존도 | 높음 | 낮음 | 낮음 | 높음 | 높음 | 중간→낮음 |
| 인프라 비용 | 없음 | 중간 | 높음 | 없음 | 낮음(고정IP) | 중간 |
| 접근 가능성 | 확실 | 확실 | 확실 | 불확실 | 조건부(사업자) | 확실 |
| 이용약관 호환성 | 양호 | 양호 | 양호 | 제한적 | **비호환**(공개서비스 금지) | 양호 |
| 확장성 | 제한적 | 높음 | 높음 | 높음 | 높음 | 높음 |
